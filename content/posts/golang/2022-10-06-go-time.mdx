fromDate = time.Now().Add(time.Hour * time.Duration(CU_DUE_DAYS*24*(-1))).Format("20060102")

fmt.Println(time.Now().Add((-1) * time.Hour * 24 * 3).Format(common.DateLayout8))
fmt.Println( toDayDate.AddDate(0,0,-3).Format(common.DateLayout8) )